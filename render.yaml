services:
  - type: web
    name: oriondeck-app
    runtime: node
    plan: starter
    buildCommand: npm run build
    startCommand: node backend/server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false

